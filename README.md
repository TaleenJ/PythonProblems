# PythonProblems
How to run Program 1
- Load the file into Visual Studio Code as a source file then run 
- "python3 Program1.py in2a.txt"

How to run Program 2
- Load the file into Visual Studio Code as a source file then run 
- "Python3 Program2.py"

How to run Program 3
- Load the file into Visual Studio Code as a source file then run 
- "python3 Program3.py"

Program1: 
Input: A file containing a string of lowercase letters and an array of string and the file ends in .txt
Output: An array with indexes of where the word was found and an array of the cities found.

Program2:
Compresses consecutive repeated characters into a count plus the character.

Program3:
Merges multiple sorted arrays into one sorted list using a min-heap.
